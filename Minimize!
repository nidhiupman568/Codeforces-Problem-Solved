#define _CRT_SECURE_NO_WARNINGS

#include <iostream>
#include <vector>
#include <algorithm>
#include <map>
#include <unordered_map>
#include <array>
#include <string>
#include <math.h>
#include <cstdint>
#include <fstream>
#include <set>
#include <unordered_set>
#include <iterator>
#include <functional>



using namespace std;

//DEFINITIONS
#define fl(x, sv, len) for(int x = sv; x < len; x++)
#define flm(x, sv, len) for(int x = sv; x > len; x--)
#define f first
#define s second
#define pi = 3.14
using ll = long long;
using vll = vector<ll>;
using vi = vector<int>;
using vii = vector<vector<int>>;
using vpi = vector<pair<int, int>>;
using pl = pair<int, int>;

void solve() {
    int a, b; cin >> a >> b;


    cout << b - a << '\n';

}

int main() {
	ios_base::sync_with_stdio(0); cin.tie(0);
    //freopen("convention2.in", "r", stdin);
	//freopen("convention2.out", "w", stdout);

	int t;
	cin >> t;
	while (t--) {
		solve();
	}
    //solve();

	return 0;
}
