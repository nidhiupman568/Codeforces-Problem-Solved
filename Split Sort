#include<bits/stdc++.h>
using namespace std;

int main(){
	int t,n;
	cin>>t;
	while(t--){
		cin>>n;
		int k=0;
		map<int,int> aux;
		for(int i=0,x; i<n; i++){ 
			cin>>x; 
			if(aux[x+1]){k++;}
			aux[x]++;		
		}
		cout<<k<<endl;
	}
}
